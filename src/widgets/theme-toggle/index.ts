/**
 * Theme Toggle Widget - Public API
 *
 * Note: ModeToggle has been moved to @/shared/ui/mode-toggle
 * for use by multiple widgets. Re-exporting here for backward compatibility.
 */

export { ThemeProvider } from './ui';

// Re-export ModeToggle from shared layer for backward compatibility
export { ModeToggle } from '@/shared/ui/mode-toggle';
